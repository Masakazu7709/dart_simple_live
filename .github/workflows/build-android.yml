name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.x"
          cache: true

      # 1. 使用标准的 Python 脚本操作 YAML 节点，绝对不会发生格式与缩进错位
      - name: Safely Patch pubspec.yaml
        run: |
          cd simple_live_app
          python3 -c "
          path = 'pubspec.yaml'
          with open(path, 'r', encoding='utf-8') as f:
              content = f.read()

          # 替换 intl 约束
          content = content.replace('intl: ^0.20.2', 'intl: any')

          # 在 dependencies: 节点下插入 auto_orientation
          if 'auto_orientation:' not in content:
              content = content.replace('dependencies:\n', 'dependencies:\n  auto_orientation: ^2.2.1\n')

          # 在文件末尾确保顶级追加 dependency_overrides
          if 'dependency_overrides:' not in content:
              content += '\ndependency_overrides:\n  dynamic_color: 1.7.0\n'

          with open(path, 'w', encoding='utf-8') as f:
              f.write(content)
          "

      # 2. 清理写死/残留的 GeneratedPluginRegistrant.java，防止 Java 编译报找不到类
      - name: Clean Outdated Plugin Registrant
        run: |
          cd simple_live_app
          rm -f android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          flutter clean

      # 3. 获取依赖
      - name: Restore Packages
        run: |
          cd simple_live_app
          flutter pub get

      # 4. 构建 APK
      - name: Build APK
        run: |
          cd simple_live_app
          flutter build apk --release --no-tree-shake-icons --android-skip-build-dependency-validation

      # 5. 上传安装包
      - name: Upload APK to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: simple_live_app/build/app/outputs/flutter-apk/*.apk
